<div class="container mt-5">
	<div class="row">
		<div class="col-sm-12">
			<%= simple_form_for @classroom do |f| %>
				<%= f.input :title %>
				<%= f.submit "Salvar" %>
			<% end %>
		</div>
	</div>
</div>