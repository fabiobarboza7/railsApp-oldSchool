// app/views/subjects/create.js.erb
// Here you generate *JavaScript* that would be executed in the browser
function showCourse(courseHTML) {
  const subjects = document.getElementById('ajax-content');
  $(subjects).html(courseHTML);
}
showCourse('<%= j render "subjects/index", subject: @subjects %>');

