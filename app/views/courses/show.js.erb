// app/views/courses/create.js.erb
// Here you generate *JavaScript* that would be executed in the browser
function showCourse(courseHTML) {
  const courses = document.getElementById('ajax-content');
  $(courses).html(courseHTML);
}
showCourse('<%= j render "courses/show", course: @course %>');

