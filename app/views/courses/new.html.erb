<% content_for(:title_tag) do %>
  Cadastrar Curso
<% end %>
<% if policy(@course).new? %>
  <header class="banner-moodev d-flex justify-content-center align-items-center">
    <div class="container">
      <div class="row">
        <div class="col-sm-6 offset-sm-3">
          <h3 class="text-center">Cadastrar Curso</h3>
          <%= render 'form', course: @course %>
        </div>
      </div>
    </div>
  </header>
<% end %>
